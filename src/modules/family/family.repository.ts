// TODO: Implement family database access layer.
//
// Responsibilities:
//   - findFamilyById(familyId): fetch family with member users
//   - updateFamily(familyId, data): update family fields
//   - createUser(data): add a new user to an existing family
//   - deleteUser(userId, familyId): remove a user (validate familyId for RLS)
//
// All queries must be scoped to familyId — never return data across family boundaries.

export class FamilyRepository {
  // TODO: implement
}
