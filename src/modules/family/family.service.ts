// TODO: Implement family business logic.
//
// Responsibilities:
//   - getFamily(familyId): return family with its members
//   - updateFamilyName(familyId, name): update family name — ADMIN only
//   - inviteMember(familyId, email): create a pending invitation (V1: create user directly
//     with a temporary password or magic link flow — TBD in implementation)
//   - removeMember(familyId, targetUserId, requestingUserId): validate that the
//     requesting user is ADMIN and not removing themselves
//
// Returns Result<T, AppError> — never throws across module boundaries.

export class FamilyService {
  // TODO: implement
}
